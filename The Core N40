bool solution(vector<int> arr) 
{
    int middle;
    if(arr.size() % 2 == 0)
        middle = arr[arr.size() / 2 - 1] + arr[arr.size() / 2];
    else 
        middle = arr[arr.size() / 2];
    return arr[0] == middle && arr[arr.size() - 1] == middle; 
}
