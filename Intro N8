int solution(vector<vector<int>> matrix) 
{
	int sum = 0;
    for(int i = 0; i < matrix[0].size(); i++)
        for(int j = 0; j < matrix.size() && matrix[j][i]; j++)
            sum += matrix[j][i];
    return sum;
}
