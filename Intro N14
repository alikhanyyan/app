vector<int> solution(vector<int> a) 
{
    vector<int> array = {0, 0};
    for(int i = 0; i < a.size(); i++)
        if(i % 2 == 0)
            array.at(0) += a[i];
        else
            array.at(1) += a[i];
    return array;
}
